{
  "name": "api",
  "description": "Backend API for sendsh.it",
  "private": true,
  "scripts": {
    "build": "cd ~/go/src/api && make",
    "deploy": "AWS_PROFILE=personal sls deploy --stage production",
    "start": "sam local start-api --profile personal"
  },
  "author": "Ben Speakman",
  "license": "MIT",
  "devDependencies": {
    "serverless-sam": "0.0.5"
  }
}
